/** 대체 위치 안내(정적) — 셀프 주차 등록 화면(VIS-01)이 아직 없어 문구로만 안내한다. */
export function AltLocationGuide() {
  return (
    <div className="rounded-card bg-surface-sunken p-4 text-center">
      <p className="text-sm text-ink-muted">행사장 위치는 초대 문자에 첨부된 지도를 참고해 주세요.</p>
    </div>
  );
}
